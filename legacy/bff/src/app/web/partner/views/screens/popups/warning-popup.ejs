<div class="custom-overlay">
    <div class="popup one-btn-popup">
        <div class="popup-wrapper">
            <div class="center-data">
                <div class="icon">
                    <% if(data.valid) { %>
                    <img src="/public/partner/img/confirm.svg" alt="check" />
                    <% } else{ %>

                    <img src="/public/partner/img/wrong.svg" alt="wrong" />
                    <% } %>
                </div>

                <div class="title"><%= data.title %></div>
                <div class="notification">
                    <%= data.subtitle %>
                </div>
            </div>
            <div class="btn-wrapper">
                <button class="btn primary-green-btn equal-width" id="confirmPopupYes">تأكيد</button>
                <button class="btn secondary-gray-btn" id="hidePopup">إلغاء</button>
            </div>
        </div>
    </div>
</div>