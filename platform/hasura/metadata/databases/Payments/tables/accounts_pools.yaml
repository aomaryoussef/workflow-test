table:
  name: pools
  schema: accounts
array_relationships:
  - name: pool_accounts
    using:
      foreign_key_constraint_on:
        column: pool_id
        table:
          name: pool_accounts
          schema: accounts
